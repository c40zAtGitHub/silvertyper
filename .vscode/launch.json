{
    "configurations": [
        {
            "name": "Python Debugger: At Cur Dir",
            "type": "debugpy",
            "request": "launch",
            "program": "${file}",
            "cwd": "${fileDirname}",
            "justMyCode": false,
        }
    ]
}